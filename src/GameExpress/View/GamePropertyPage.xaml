<Page
    x:Class="GameExpress.View.GamePropertyPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GameExpress.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    mc:Ignorable="d">

    <StackPanel Padding="10,10,10,10">
        <controls:HeaderedItemsControl Header="Name:" >
            <TextBox Name="Name" Margin="0,10,0,10" Text="{Binding Name, Mode=TwoWay}"/>
        </controls:HeaderedItemsControl>

        <controls:HeaderedItemsControl Header="Beschreibung:" >
            <RichEditBox Name="Note" Margin="0,10,0,10"/>
        </controls:HeaderedItemsControl>

        <controls:HeaderedItemsControl Header="Breite:" >
            <TextBox Name="Width" Margin="0,10,0,10" Text="{Binding Width, Mode=TwoWay}"/>
        </controls:HeaderedItemsControl>

        <controls:HeaderedItemsControl Header="Höhe:" >
            <TextBox Name="Height" Margin="0,10,0,10" Text="{Binding Height, Mode=TwoWay}"/>
        </controls:HeaderedItemsControl>
        
    </StackPanel>
</Page>
