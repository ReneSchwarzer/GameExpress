<Page
    x:Class="GameExpress.View.StoryPropertyPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    mc:Ignorable="d">

    <StackPanel Padding="10,10,10,10" HorizontalAlignment="Stretch">
        <TextBox Header="Name:" Name="Name" Margin="0,10,0,10" Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
        <controls:HeaderedContentControl Margin="0,10,0,10" Header="Instanz:" HorizontalAlignment="Stretch">
            <Grid HorizontalAlignment="Stretch">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Name="Instance" Margin="0,0,10,0" Text="{Binding Item, Mode=TwoWay}"/>
                <Button Grid.Column="1" Click="OnChangeInstance">
                    <SymbolIcon Symbol="Link"/>
                </Button>
            </Grid>
        </controls:HeaderedContentControl>

    </StackPanel>
</Page>
