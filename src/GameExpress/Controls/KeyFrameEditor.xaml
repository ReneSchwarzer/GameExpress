<UserControl
    x:Class="GameExpress.Controls.KeyFrameEditor"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.Graphics.Canvas.UI.Xaml"
    mc:Ignorable="d"
    Loaded="OnLoaded"
    Unloaded="OnUnloaded"
    SizeChanged="OnSizeChanged"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <controls:CanvasControl Name="Content"
                            VerticalAlignment="Stretch"
                            Background="{Binding Background, RelativeSource={RelativeSource Mode=TemplatedParent}}"
                            Draw="OnDraw"
                            CanDrag="False"
                            PointerPressed="OnPointerPressed"
                            PointerMoved="OnPointerMoved"
                            PointerReleased="OnPointerReleased"
                            PointerExited="OnPointerExited"
                            PointerCanceled="OnPointerCanceled"
                            DragStarting="OnDragStarting"/>
</UserControl>
